// Compatibility stub after the matching system was removed.

export const REVIEW_THRESHOLD = 0;

export function mostSimilar(_path: string, _candidates: string[]): string | undefined {
  return undefined;
}

export function similarity(_a: string, _b: string): number {
  return 0;
}
